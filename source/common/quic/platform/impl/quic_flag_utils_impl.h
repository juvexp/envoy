#ifndef GFE_QUIC_PLATFORM_IMPL_QUIC_FLAG_UTILS_IMPL_H_
#define GFE_QUIC_PLATFORM_IMPL_QUIC_FLAG_UTILS_IMPL_H_

// TODO: Implement quic_flag_utils_impl.h
#define QUIC_FLAG_COUNT_IMPL(flag) do {} while (0)
#define QUIC_FLAG_COUNT_N_IMPL(flag, instance, total) do {} while (0)

#define QUIC_CODE_COUNT_IMPL(name) do {} while (0)
#define QUIC_CODE_COUNT_N_IMPL(name, instance, total) do {} while (0)

#endif  // GFE_QUIC_PLATFORM_IMPL_QUIC_FLAG_UTILS_IMPL_H_
